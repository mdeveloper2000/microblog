<%- include("./partials/header"); -%>

<div class="container">        
    <div class="row news">
        <% news.forEach((item) => { %>
            <div class="col-12">
                <div class="card">
                    <p><%= item.content %></p>
                </div>
            </div>
        <% }) %>
    </div>
</div>
    
</body>
</html>